@layout('layouts.main')

@section('extracss')
@endsection

@section('title')
Previous Shows
@endsection

@section('content')
    <div class="show_display">
        @each(show in shows)
          <div class="database_entry">
            <a href="/future-shows/{{show.id}}">
              Name: {{show.Show_title}}<br>Date: {{show.Show_date}}</a>
            <div class="links">
              <a href="/future-shows/edit-show/{{show.id}}" class = "btn">Edit</a>
              <a href="/future-shows/delete/{{show.id}}" class = "btn">Delete</a>
            </div>
          </div>
           <div class="right">
              <form action=" {{ route('ShowController.oops', { id: show.id}) }}" method="POST">
                {{ csrfField() }}
                <button type="submit">
                  Oops
                </button>
              </form>
            </div>
        @endeach
    </div>
@endsection
